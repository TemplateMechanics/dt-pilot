# GENERATED FILE - do not hand-edit. Regenerate with ./scripts/terraform/Sync-TerraformCatalog.ps1
# SPDX-License-Identifier: MIT

variable "dashboard_name" {
  type        = string
  description = "Display name shown in the Dynatrace UI. Lives in the nested dashboard_metadata { name = ... } block -- no top-level provider argument."
}

variable "owner" {
  type        = string
  description = "Owner email or service-account identifier. Lives in the nested dashboard_metadata { owner = ... } block -- no top-level provider argument."
}
