# GENERATED FILE - do not hand-edit. Regenerate with ./scripts/terraform/Sync-TerraformCatalog.ps1
# SPDX-License-Identifier: MIT
# Scaffold for Auto-Tag Rule (v2) (dynatrace_autotag_v2).
# Copy this file to your project's main.tf (or merge in) and fill the TODO markers.

resource "dynatrace_autotag_v2" "TODO_local_name" {
  name = var.tag_name
  rule_expression = var.rule_expression  # TODO: this variable feeds a nested block or has no top-level provider arg -- move into the right block before applying
  # TODO: add the resource-specific blocks from the provider docs.
}
